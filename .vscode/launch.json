{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run app (CMake Debug)",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/Debug/your_executable.exe",
      "args": ["../config.yaml"],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "CMake: configure+build (MSVC)"
    }
  ]
}
    